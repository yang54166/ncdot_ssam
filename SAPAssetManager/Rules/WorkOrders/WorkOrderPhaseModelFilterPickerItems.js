import PhaseModelFilterPickerItems from '../PhaseModel/PhaseModelFilterPickerItems';

export default function WorkOrderPhaseModelFilterPickerItems(context) {
    return PhaseModelFilterPickerItems(context, 'ORI');
}
