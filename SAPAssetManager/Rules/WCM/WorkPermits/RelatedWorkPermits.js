import RelatedWorkPermitsReadLink from './RelatedWorkPermitsReadLink';

export default function RelatedWorkPermits(sectionProxy) {
    let context = sectionProxy.getPageProxy();
    return RelatedWorkPermitsReadLink(context);
}
